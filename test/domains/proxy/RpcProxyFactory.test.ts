import { RpcProxyFactory } from "../../../src/domains/proxy/RpcProxyFactory";
// import { isAsyncFunction } from 'util/types';

describe('RpcProxyFactory', () => {
  describe('proxy', () => {
    it('should proxy the target', () => {
      // given
      
      // when
      // const rpcProxyFactory = new RpcProxyFactory();

      // then

    });
  });
});